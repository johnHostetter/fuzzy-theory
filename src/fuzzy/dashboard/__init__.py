"""
Optional visualization/dashboard tooling for fuzzy-theory (the `dashboard` extra).

- activation_viewer.py: a self-contained static-HTML report of a captured
  torch.nn.Module's activations (matplotlib-based, no server).
- flc_viewer.py: an interactive Dash + dash-cytoscape app for exploring a single
  captured FuzzyLogicController forward pass (see fuzzy.logic.control.trace).

Prototype status: this subpackage is still taking shape.
"""
